---
import { Image } from 'astro:assets';

const { icon, width="25", height="25" } = Astro.props;
---

<actionicon>
    <Image alt=`${icon}` class="lost-action-icon" loading="eager" width=`${width}` height=`${height}` src=`/drs/actions/${icon}.png`` />
</actionicon>